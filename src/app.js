import React,{Component} from 'react'
// React,{Component} 其中React是接收导出的变量。
// Component为React中的属性，将它单独存入变量
import ReactDom from 'react-dom'

export default class extends React.Component{
    render(
        return (<div>
                123
            </div>)
    )
}